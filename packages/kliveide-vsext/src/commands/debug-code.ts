import { OutputChannel } from "vscode";

/**
 * Compiles the code, injects it into the virtual machine, and then, starts it
 * in debug mode
 */
export function debugCode(output: OutputChannel): void {
}
