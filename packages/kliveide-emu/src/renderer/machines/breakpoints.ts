import { BreakpointDefinition } from "../../shared/machines/api-data";

/**
 * Set up the current breakpoint definitions
 * @param bps Breakpoint definitions
 */
export function setupBreakpoints(bps: BreakpointDefinition[]): void {

}