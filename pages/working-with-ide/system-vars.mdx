import Image from 'next/image'
import sysvarView from '../../public/images/working-with-ide/sysvar-view.png'
import sysvarExplain from '../../public/images/working-with-ide/sysvar-explain.png'
import sysvarFlag from '../../public/images/working-with-ide/sysvar-flag.png'

# The System Variables View

> **Note**: The current System Variables view does not allow editing the variables' contents.

You can display the current system variable values in the Machine info activity tag with the **System Variables** header:

<Image src={sysvarView} alt="System variables view" width={256} style={{marginTop: 24}} />

When you hover the mouse over a particular system variable label, you can read the explanation of that variable:

<Image src={sysvarExplain} alt="System variable explanation" width={384} style={{marginTop: 24}} />

When a system variable contains multiple flags, you can point to a particular flag to display its explanation:

<Image src={sysvarFlag} alt="System variable flags" width={384} style={{marginTop: 24}} />
