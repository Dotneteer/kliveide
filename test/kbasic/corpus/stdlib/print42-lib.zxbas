' print42.bas: its cursor moves 42 columns to a row and wraps, apart from PRINT's
'@expect screen 0 "xy   2 4,1"
#include <print42.bas>
#include <pos.bas>
DIM c AS UByte
PRINT "xy";
printat42(3, 40)
print42("abc")
c = POS()
PRINT AT 0, 5; c; " "; __kbP42Row; ","; __kbP42Col
