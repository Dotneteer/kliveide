import { NextIoPortManager } from "@emu/machines/zxNext/IoPorts/NextIoPortManager";
import { describe, it, expect } from "vitest";

describe("PagedMemory", () => {
  it("constructor works", () => {
    const portManager = new NextIoPortManager();
  });
});
