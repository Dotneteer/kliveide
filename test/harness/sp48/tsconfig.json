{
  // Type-checks the 48K harness and the Klive BASIC tests that use it:
  //   npx tsc -p test/harness/sp48/tsconfig.json
  "extends": "../../tsconfig.json",
  "compilerOptions": { "noEmit": true, "lib": ["ESNext", "DOM"] },
  "include": ["./**/*.ts", "../../kbasic/**/*.ts", "../../ai-notes/**/*.ts"],
  "exclude": ["node_modules"]
}
