import { AppContext } from "@/core/abstractions"

const BreakpointsPanel = () => {
 return <div>Breakpoints</div>   
}

export const breakpointsPanelRenderer = (context: AppContext) => <BreakpointsPanel />