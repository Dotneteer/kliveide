/**
 * Defines theming properties for the application
 */
export interface Theme {
  colors: {
    primary: string
    secondary: string
    background: string
    text: string
  }
  spacing: {
    small: string
    medium: string
    large: string
  }
}
