' Klive BASIC standard library - sinclair.bas: the Sinclair BASIC functions that are library code
' here (ATTR, POINT and SCREEN$). `sinclair-compatible` includes it before the program's first line.
' Klive's own code (plan §6.4).
#pragma once
#include <attr.bas>
#include <point.bas>
#include <screen.bas>
